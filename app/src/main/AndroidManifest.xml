<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="unme.app.com.ume">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LandingPageActivity" />
        <activity android:name=".EditUserActivity" />
        <activity android:name=".LandingPage2Activity" />
        <activity android:name=".CountdownActivity" />
        <activity android:name=".TodoList" />
        <activity android:name=".GuestViewActivity" />
        <activity android:name=".AddServiceActivity" />
        <activity android:name=".MyAllServicesActivity" />
        <activity android:name=".EditServiceActivity" />
        <activity android:name=".SearchServicesActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".MyServiceListActivity" />
        <activity android:name=".ViewServiceActivity" />
        <activity android:name=".MyListActivity" />
        <activity android:name=".MyConfirmList"></activity>
    </application>

</manifest>